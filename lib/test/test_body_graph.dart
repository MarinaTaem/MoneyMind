import 'package:flutter/material.dart';

class TestBodyGraph extends StatelessWidget {
  const TestBodyGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Test Body Graph'),
      ),
    );
  }
}
